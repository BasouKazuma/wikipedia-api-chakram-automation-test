
before(function() {
  // runs before all tests in every file are completed
})

after(function() {
  // runs after all tests in every file are completed 
})

beforeEach(function() {
  // runs before each test in every file
})

afterEach(function() {
  // runs after each test in every file
})
